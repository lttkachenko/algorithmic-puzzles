export const stringIsAnagram1Liner = (str1: string, str2: string): boolean => {
  return true;
};

export const stringIsAnagram = (str1: string, str2: string): boolean => {
  return true;
};

export const stringIsPalindrome = (str: string): boolean => {
  return false;
};
