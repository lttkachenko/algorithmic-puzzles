import { ITree } from "../types";

export const treeSum = (tree: ITree<unknown>, verbose = false): unknown => {
  return true;
};

export const treeSearch = (tree: ITree<unknown>, value: unknown, verbose = false): unknown => {
  return true;
};
