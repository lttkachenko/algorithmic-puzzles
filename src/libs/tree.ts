import { ITree } from "../types";

export const treeSum = (tree: ITree<unknown>): unknown => {
  return true;
};

export const treeSearch = (tree: ITree<unknown>, value: unknown): unknown => {
  return true;
};
